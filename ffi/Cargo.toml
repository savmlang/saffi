[package]
name = "saffi"
version = "0.1.1"
edition = "2024"
license = "Apache-2.0"

[dependencies]
salloc = { package = "salloc-sys", path = "../salloc" }
tokio = { version = "^1", features = ["rt", "sync", "time", "macros"] }
